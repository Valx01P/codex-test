interface:
  display_name: "Codex Test"
  short_description: "Choose, improve, validate, and explain tests"
  brand_color: "#2563EB"
  default_prompt: "Use $codex-test to show the numbered testing workflow menu, wait for my choice, improve this repo's tests, validate the changes, and write CODEX-TEST-REPORT.md."

policy:
  allow_implicit_invocation: true
